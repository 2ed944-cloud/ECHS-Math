# AP Calculus 1.5 — interactive limit laws with preserved challenges

Lesson 1.5 now follows the interactive ECHS slide format used in Lessons 1.1–1.4 and 1.6. There are 65 slides, eight investigations, 58 objective checks, and 18 FRQs. The AP core leads to 16 original AP-style MCQs, three FRQs and an exit check. A separate section retains 36 challenge MCQs and 15 challenge FRQs, including optional formal proofs.

The core follows AP Topic 1.5, LIM-1.D.1–2 and suggested mathematical practice 1.E: finite limit laws, analytical/graphical one-sided limits, justified direct substitution and composite limits. Every quotient checks its nonzero denominator condition. Root examples distinguish the actual real domain, a one-sided domain and a full punctured neighborhood. Composition checks the inner limiting input and outer continuity, with directional alternatives reserved for extension practice. Topic 1.6 is identified as the next step for algebraic manipulation; a squeezing example and epsilon-delta proofs are explicitly optional extensions. Scope was checked against the College Board AP Calculus AB and BC Course and Exam Description, PDF page 42, in September 2026.

All 47 former Challenge and 26 Beyond AP entries are tracked in the question data's `preservedSources` map. Repeated versions are consolidated into the same retained task. The retained collection includes the original numerical combinations, the eight graph-reading problems, both additional graph pairs, selected-value table, point-value/composition table, reverse laws, parameters, denominator counterexamples, floor-function cases, root theorem audit, nested/directional compositions and formal proofs. The former F1 and general law-audit tasks are also retained in AP practice where their content fits the core.

Mathematical corrections are explicit. Finite tables support estimates unless exact component limits are established independently. A discontinuous outer function requires tracking the actual inner approach and any repeated hits of its target value. In the original g(25−x²) problem as x→5⁺, the inner input tends to 0 from below, not to the outer jump at −1. Reverse-law tasks derive missing limits using exact identities instead of assuming those limits first. The real-root theorem is stated on its admissible domain, and uniqueness is proved directly by the epsilon-delta argument before the difference-law interpretation. Graph-reading composites now state the outer hypotheses needed for their conclusions.

Investigations cover a limit-law machine, one-sided graph combinations, root domains, three 0/0 outcomes, substitution decisions, continuous composition, reverse equations, and a discontinuous outer function. The retained graph pairs are recreated with exact native curves, readable scales and explicit open/filled points; the eight-problem set uses its preserved exact readings. The shared engine retains hints, attempt-gated solutions, account-scoped drafts, self-assessment rubrics, navigation, print and protected progression. It does not automatically grade written reasoning or award course mastery.

Mathematics checks validate all 58 answer keys, 18 three-part rubrics, the 73-source preservation map, 163 authored equations, exact graph readings, root domains, one-sided products/quotients, directional composition, reverse identities, counterexamples and proof bounds. DOM fixtures execute all eight investigations, all objective answers and 54 rubric parts, with actual local KaTeX parsing, input invalidation, account isolation, storage denial, reset, navigation, query retention and protected continuation. Access, visibility and local-link contracts are also checked. The deployment workflow runs the new tests alongside the existing lesson gates. No browser automation or production student account is used.

The canonical lesson URL is unchanged. Course metadata and the service-worker cache version are updated. Companion notes and worksheets remain separate resources. Questions and rubrics are original classroom materials, not official College Board exam items or scoring guidelines.
