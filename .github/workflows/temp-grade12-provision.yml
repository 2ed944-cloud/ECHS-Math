name: Temporary Grade 12 Provisioning Session

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/temp-grade12-provision.yml"

permissions:
  contents: read

concurrency:
  group: temporary-grade12-provisioning-session
  cancel-in-progress: false

jobs:
  issue-session:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: institutional-production
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SESSION_PUBLIC_KEY_B64: ""
    steps:
      - name: Confirm protected credentials
        shell: bash
        run: |
          set -euo pipefail
          for name in SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY SESSION_PUBLIC_KEY_B64; do
            if [ -z "${!name:-}" ]; then
              echo "Required protected value is unavailable: $name" >&2
              exit 1
            fi
          done

      - name: Issue encrypted 30-minute session
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          printf '%s' "$SESSION_PUBLIC_KEY_B64" | base64 --decode > recipient.pem
          python - <<'PY'
          import datetime
          import hashlib
          import json
          import os
          import secrets
          import urllib.parse
          import urllib.request

          base = os.environ["SUPABASE_URL"].rstrip("/")
          key = os.environ["SUPABASE_SERVICE_ROLE_KEY"]
          headers = {
              "apikey": key,
              "authorization": f"Bearer {key}",
              "content-type": "application/json",
          }

          query = urllib.parse.urlencode(
              {
                  "select": "id",
                  "role": "eq.admin",
                  "status": "eq.active",
                  "order": "created_at.asc",
                  "limit": "1",
              },
              safe=",",
          )
          request = urllib.request.Request(
              f"{base}/rest/v1/accounts?{query}",
              headers=headers,
          )
          with urllib.request.urlopen(request, timeout=60) as response:
              accounts = json.load(response)
          if not accounts:
              raise SystemExit("No active administrator account is available")

          token = secrets.token_urlsafe(48)
          token_hash = hashlib.sha256(token.encode("utf-8")).hexdigest()
          expires = datetime.datetime.now(datetime.timezone.utc) + datetime.timedelta(minutes=30)
          rpc_body = json.dumps(
              {
                  "p_account_id": accounts[0]["id"],
                  "p_token_hash": token_hash,
                  "p_expires_at": expires.isoformat(),
                  "p_user_agent_hash": hashlib.sha256(b"codex-temporary-provisioner").hexdigest(),
                  "p_ip_hash": hashlib.sha256(b"github-actions").hexdigest(),
              }
          ).encode("utf-8")
          rpc_request = urllib.request.Request(
              f"{base}/rest/v1/rpc/api_create_session",
              data=rpc_body,
              method="POST",
              headers=headers,
          )
          with urllib.request.urlopen(rpc_request, timeout=60) as response:
              response.read()

          with open("session.json", "w", encoding="utf-8") as handle:
              json.dump({"token": token, "expires_at": expires.isoformat()}, handle)
          PY
          openssl pkeyutl -encrypt             -pubin             -inkey recipient.pem             -in session.json             -out secure-session.bin             -pkeyopt rsa_padding_mode:oaep             -pkeyopt rsa_oaep_md:sha256
          shred -u session.json
          rm -f recipient.pem
          test -s secure-session.bin
          echo "Encrypted temporary session issued."

      - name: Upload encrypted session
        uses: actions/upload-artifact@v4
        with:
          name: temporary-grade12-session-${{ github.run_id }}
          path: secure-session.bin
          if-no-files-found: error
          retention-days: 1

