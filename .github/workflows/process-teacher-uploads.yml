name: Process Teacher Uploads

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      request_id:
        description: "Optional teacher_upload_requests UUID; leave blank for the oldest queued or recoverable request"
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: teacher-upload-processor
  cancel-in-progress: false

jobs:
  process:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: institutional-production
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      REQUEST_ID: ${{ inputs.request_id || '' }}
      GH_TOKEN: ${{ github.token }}
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Confirm protected processor secrets
        shell: bash
        run: |
          set -euo pipefail
          for name in SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY; do
            if [ -z "${!name:-}" ]; then
              echo "Missing repository/environment secret: $name" >&2
              exit 1
            fi
          done

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate processor source
        run: |
          python -m py_compile tools/process_teacher_upload_request.py
          python -m py_compile tools/process_teacher_upload_request_fast.py
          python -m py_compile tools/process_ib_lesson_release.py
          python -m py_compile tools/test_course_release_changed_file_selection.py
          python -m py_compile tools/upload_private_bank_package.py
          python -m py_compile tools/upload_private_bank_package_fast.py
          python tools/test_course_release_changed_file_selection.py

      - name: Reconcile existing course release pull request
        id: reconcile
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os, subprocess, urllib.parse, urllib.request
          from datetime import datetime, timezone

          base = os.environ['SUPABASE_URL'].rstrip('/')
          key = os.environ['SUPABASE_SERVICE_ROLE_KEY']
          requested = os.environ.get('REQUEST_ID', '').strip()
          select = 'id,status,upload_kind,github_pr_url'
          if requested:
              query = urllib.parse.urlencode({'select': select, 'id': f'eq.{requested}', 'limit': '1'}, safe=',')
          else:
              query = urllib.parse.urlencode({'select': select, 'status': 'eq.processing', 'upload_kind': 'eq.course-release', 'order': 'created_at.asc', 'limit': '1'}, safe=',')
          req = urllib.request.Request(f'{base}/rest/v1/teacher_upload_requests?{query}', headers={'apikey': key, 'authorization': f'Bearer {key}'})
          with urllib.request.urlopen(req, timeout=60) as response:
              rows = json.load(response)
          output = os.environ['GITHUB_OUTPUT']
          if not rows or rows[0].get('upload_kind') != 'course-release':
              with open(output, 'a', encoding='utf-8') as handle: handle.write('reconciled=false\n')
              raise SystemExit(0)

          row = rows[0]
          branch = f"teacher-release/{row['id']}"
          result = subprocess.run(['gh', 'pr', 'list', '--head', branch, '--state', 'all', '--limit', '1', '--json', 'url,state,mergedAt'], check=True, text=True, capture_output=True)
          prs = json.loads(result.stdout or '[]')
          if not prs:
              with open(output, 'a', encoding='utf-8') as handle: handle.write('reconciled=false\n')
              raise SystemExit(0)

          pr = prs[0]
          merged = bool(pr.get('mergedAt'))
          values = {'status': 'completed' if merged else 'pr-opened', 'progress': 100 if merged else 92, 'stage': 'Course release merged and published' if merged else 'Pull request opened · awaiting review and merge', 'github_pr_url': pr['url'], 'updated_at': datetime.now(timezone.utc).isoformat(), 'error_message': None}
          if merged: values['completed_at'] = datetime.now(timezone.utc).isoformat()
          patch_query = urllib.parse.urlencode({'id': f"eq.{row['id']}"})
          body = json.dumps(values).encode()
          patch = urllib.request.Request(f'{base}/rest/v1/teacher_upload_requests?{patch_query}', data=body, method='PATCH', headers={'apikey': key, 'authorization': f'Bearer {key}', 'content-type': 'application/json', 'prefer': 'return=minimal'})
          urllib.request.urlopen(patch, timeout=60).read()
          with open(output, 'a', encoding='utf-8') as handle:
              handle.write('reconciled=true\n')
              handle.write(f"request_id={row['id']}\n")
              handle.write(f"pr_url={pr['url']}\n")
              handle.write(f"merged={'true' if merged else 'false'}\n")
          print(f"Reconciled {row['id']} from {pr['url']} · merged={merged}")
          PY

      - name: Process oldest queued request
        id: processor
        if: steps.reconcile.outputs.reconciled != 'true'
        shell: bash
        run: |
          set -euo pipefail
          args=()
          if [ -n "$REQUEST_ID" ]; then args+=(--request-id "$REQUEST_ID"); fi
          python -u tools/process_teacher_upload_request_fast.py "${args[@]}"

      - name: Stop when queue is empty
        if: steps.reconcile.outputs.reconciled != 'true' && steps.processor.outputs.found != 'true'
        run: echo "No queued teacher uploads."

      - name: Validate generated course or lesson release
        if: steps.processor.outputs.changed == 'true'
        shell: bash
        run: |
          set -euo pipefail
          git diff --check

          # A lesson package may deliberately replace an older release layout. Deleted
          # legacy paths are valid repository changes, but they are not files that can
          # be opened by grep or node. Validate only existing added, copied, modified,
          # renamed, and untracked files produced by the current processor run.
          changed_files="$({
            git diff --name-only --diff-filter=ACMR --
            git ls-files --others --exclude-standard
          } | sed '/^$/d' | sort -u)"
          deleted_files="$(git diff --name-only --diff-filter=D -- | sed '/^$/d' | sort -u)"
          changed_html="$(printf '%s\n' "$changed_files" | grep -E '^lessons/.+\.html$' || true)"
          changed_js="$(printf '%s\n' "$changed_files" | grep -E '^data/(ap-precalculus|ib-math-ai)-unit-[0-9]+-update\.js$' || true)"
          changed_runtime_js="$(printf '%s\n' "$changed_files" | grep -E '^lessons/.+\.js$' || true)"

          if [ -z "$changed_html" ]; then
            echo "Course release produced no existing lesson HTML files" >&2
            git status --short
            exit 1
          fi
          if [ -z "$changed_js" ]; then
            echo "Course release produced no AP Precalculus or IB Mathematics AI portal update file" >&2
            git status --short
            exit 1
          fi
          while IFS= read -r file; do
            [ -n "$file" ] || continue
            [ -f "$file" ] || continue
            grep -qi '<html' "$file"
            grep -qi '</html>' "$file"
          done <<< "$changed_html"
          while IFS= read -r file; do
            [ -n "$file" ] || continue
            [ -f "$file" ] || continue
            node --check "$file"
          done <<< "$changed_js"
          while IFS= read -r file; do
            [ -n "$file" ] || continue
            [ -f "$file" ] || continue
            node --check "$file"
          done <<< "$changed_runtime_js"

          if [ -n "$deleted_files" ]; then
            echo "Legacy release paths replaced by this package:"
            printf '%s\n' "$deleted_files"
          fi

      - name: Create release branch and pull request
        id: pr
        if: steps.processor.outputs.changed == 'true'
        shell: bash
        run: |
          set -euo pipefail
          request="${{ steps.processor.outputs.request_id }}"
          branch="teacher-release/${request}"
          git config user.name "ECHS Upload Manager"
          git config user.email "actions@users.noreply.github.com"
          git switch -c "$branch"
          git add lessons data index.html
          git commit -m "${{ steps.processor.outputs.pr_title }}"
          git push origin "$branch"
          url="$(gh pr create --base main --head "$branch" --title "${{ steps.processor.outputs.pr_title }}" --body "Created automatically by ECHS Lesson & Unit Release Manager.\n\nThe uploaded ZIP remained private, was verified by SHA-256, checked for unsafe paths and missing local assets, and converted into a reviewable course release.\n\nUpload request: \`${request}\`.\n\nThis pull request is never merged automatically.")"
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Record pull request in upload queue
        if: steps.processor.outputs.changed == 'true' && steps.pr.outputs.url != ''
        shell: bash
        env:
          PR_URL: ${{ steps.pr.outputs.url }}
          UPLOAD_REQUEST_ID: ${{ steps.processor.outputs.request_id }}
        run: |
          python - <<'PY'
          import json, os, urllib.parse, urllib.request
          url=os.environ['SUPABASE_URL'].rstrip('/')
          key=os.environ['SUPABASE_SERVICE_ROLE_KEY']
          rid=os.environ['UPLOAD_REQUEST_ID']
          body=json.dumps({'status':'pr-opened','progress':92,'stage':'Lesson release pull request opened · awaiting review and merge','github_pr_url':os.environ['PR_URL']}).encode()
          query=urllib.parse.urlencode({'id':f'eq.{rid}'})
          request=urllib.request.Request(f'{url}/rest/v1/teacher_upload_requests?{query}',data=body,method='PATCH',headers={'apikey':key,'authorization':f'Bearer {key}','content-type':'application/json','prefer':'return=minimal'})
          urllib.request.urlopen(request,timeout=60).read()
          PY

      - name: Summary
        if: always()
        run: |
          echo "Reconciled existing release: ${{ steps.reconcile.outputs.reconciled }}"
          echo "Reconciled PR: ${{ steps.reconcile.outputs.pr_url }}"
          echo "Found request: ${{ steps.processor.outputs.found }}"
          echo "Kind: ${{ steps.processor.outputs.kind }}"
          echo "Repository changes: ${{ steps.processor.outputs.changed }}"
          echo "Pull request: ${{ steps.pr.outputs.url }}"
