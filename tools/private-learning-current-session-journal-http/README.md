# Isolated current-session journal HTTP fixture

This is an implemented candidate; actual C01–C12 Linux HTTPS/SQL execution is pending. Its driver is designed to execute the exact selected canonical P2b client, current-session bridge/controller and T3 store through native Chromium HTTPS to unchanged J049, PostgREST and disposable PostgreSQL. The account `/me` response is a synthetic private token/profile registry. It does not execute production authentication, use a production endpoint, adopt an active page or establish cross-origin production configuration.

The accepted HTTP20 and B12 cases and their original90 local checks remain unchanged prerequisites. C01–C12 are twelve additional groups under `report-contract.json`; they are never counted as repetitions of the baseline. `selected-source.json` binds twelve served runtime scripts and one retained, unserved practice HTML file to frozen current-session manifest `660bb66c7a37cab21972dc540a32fd2df788f9756333852bb10b848bf95eef76`. Actual pages use the early P2b HTML marker and load the real canonical client before the reviewed classic entry. No test replaces authority verification or the bridge's journal ports.

The listener admits only its fixed static buffers, exact GET config/`me` routes, and the four unchanged journal POST/OPTIONS routes. Config is generated from the owned loopback HTTPS origin. Private fixture controls create actual SQL sessions and owners; the closed missing-owner variant omits only the journal-owner insert. No control API is exposed to the browser.

The new body hold waits for a native first-byte write callback before announcing entry. Its bounded read, twelve-second work deadline, abortable backpressure wait and one-second final settlement preserve earlier request errors. A sticky cleanup failure survives removal from the active hold set. `finished` provides the final closed state for group cleanup; listener cleanup fails if any claimed hold remained unsettled. Original drop/partial/pause modes are unchanged.

C06 tests composite cancellation after real SQL commit, unchanged eleven stores and exact retry. C07 alone tests the bridge's own ten-second body deadline with its caller signal deliberately un-aborted; T3's earlier deadline is not used for that attribution. C10 is an explicitly labeled valid-shape incarnation mutation of a real SQL response, not a real SQL incarnation transition. C11 combines five pinned source guard sites with native public/event behavior; public early guards make the private queue-send guard unreachable while held, so this is not five independent native branch executions. C12 uses transparent native IndexedDB request observation and bounded read-only upgrade keepalive to expose a late real open, plus a separate actual BroadcastChannel observation. It changes no retained schema or store code.

The runner retains the accepted private run root and independently owns all four process roots: Chromium, HTTP driver, baseline browser driver, and current-session driver. Both browser driver roots have distinct fixed source, output, protocol-log and listener-file bindings. Raw credentials, account bodies, journal wire strings, storage dumps and traces remain private. Failure projection is closed and grants no acceptance. The browser stderr prefix belongs to the baseline driver interval, not the later current-session phase.

The two browser drivers run sequentially in one owned Chromium 149 process and profile, with fresh isolated contexts, under the reviewed `NetworkServiceInProcess2` configuration. This fixture topology does not establish compatibility with Chromium's default out-of-process network service. The collector publishes its success index only after all final checks; safe partial metadata may remain after a failed publication, and any extra or missing member rejects acceptance.

Success requires exact source sweeps, unchanged baseline assertions, all twelve additional outcomes, native narrow-SPKI TLS plus specific unrelated-certificate rejection, independently verified SQL/runtime identity and complete process/socket/task/private cleanup. The successful service artifact has22 exact members; the original local artifact retains7. Development adapter26 and earlier current-session module34/native16 evidence remain separately labeled prerequisites. A passing isolated gate does not complete C04 or authorize active rollout.
