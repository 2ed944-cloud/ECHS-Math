# Isolated T3 implementation boundary

Frozen predecessor: c04-learning-wire-binding-candidate/source-manifest-v3.json, SHA-256 34d4640a7fb9176ce93500bdc50ce9273b3be3791f99446e79a0282b92666968. Its14 sources and all evidence remain unchanged. The fresh native database is echs-owned-learning-ack-candidate-v1, schema1; no migration, production adoption, Pages or server change.

The copied owner store adds deliverNext({operation_id?}) using an injected trusted deliveryPort.deliver({route,requestRaw,signal}). A selected ID can only refer to a validated stored wire; no caller raw body, receipt object or verified flag is accepted. Default delivery is absent. The exact persisted apply request is captured before I/O, with per-delivery deadline/cancellation and fresh owner checks. No IndexedDB transaction spans the I/O. Synthetic ports prove native mechanics only; actual browser-to-HTTP provenance remains a later gate.

The model gets explicit acknowledged wire/binding schemas. A wire retains its first raw apply response, status, exact request and V3 pending-intent result with immutable canonical receipt. Known receipt replay may update a separate generation-block observation but must not replace the first evidence. Pending validation remains strict. First lookup cannot ACK. All retained serialized fields, including raw duplication, are charged before clearing rows.

The ACK transaction rereads the exact wire/source/binding and current authority. It does not reject unrelated newer local command/row revisions. For each source row it checks the latest pending row and its immutable source/lineage. Equal revision plus equal raw source JSON is cleared; a valid newer source revision remains pending. Missing/older rows, equal-revision changed bytes, duplicate revisions, wrong tuple or invalid source linkage abort. A concurrent already-acknowledged result must validate the same immutable receipt and leave its first evidence/counters unchanged.

The same strict transaction retains wire/receipt, marks exactly its binding acknowledged, advances exactly that queue position, clears only its own in-flight pointer, and charges/removes exact bytes/counts. It never removes source bundles, commands, attempts/history, producer lineage or engine projections. It grants no grading/mastery authority and runs no local effects. Late/lost completion recovers the persisted receipt.

Later T2 materialization can resolve a captured predecessor only through its exact acknowledged source, operation and receipt record. Resolution captures expected revision once; it never reads newest remote heads. Missing/foreign/wrong-index/kind/ID/revision/context/generation evidence holds the entire dependent source. A historical receipt with a newer current reset generation remains valid evidence but blocks future old-context materialization. No reset or deletion mapping is introduced.

Files to change: source/js/owned-learning-store.mjs, source/js/wire-binding-model.mjs, browser-page.mjs, test_browser.mjs, README.md and verify_candidate.py. The three J049 V3 files and original math/practice/held transport stay byte-identical. New provenance/overview records are local. Original61 predecessor case bodies remain preserved; added cases exercise real native transactions with explicitly synthetic delivery evidence. Freeze and root/peer acceptance follow actual results.
