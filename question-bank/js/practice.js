/* ECHS Focused Practice — authenticated pathway edition */
const UI={mode:document.getElementById("mode"),group:document.getElementById("group"),bundle:document.getElementById("bundle"),bank:document.getElementById("bank"),type:document.getElementById("type"),difficulty:document.getElementById("difficulty"),section:document.getElementById("section"),count:document.getElementById("count"),start:document.getElementById("start"),status:document.getElementById("status"),shell:document.getElementById("shell"),heroLoaded:document.getElementById("heroLoaded"),heroBanks:document.getElementById("heroBanks"),heroDue:document.getElementById("heroDue"),heroMastery:document.getElementById("heroMastery")};
let catalog,loaded=[],set=[],loading=false,session=null,targetCount=10,lastResult=null,questionStartedAt=Date.now(),access=null;
let state={index:0,response:null,checked:false,correct:0,graded:0,answered:new Set()};
const params=ECHSBank.params(),assignmentId=params.get("assignment"),assignmentTitle=params.get("title"),fromLesson=params.get("from"),requestedTopic=params.get("topic");
const modeCopy={manual:["Focused practice","Questions are selected from your unlocked learning pathway."],adaptive:["Adaptive practice","The next question responds to mastery, recent evidence and suitable difficulty."],review:["Spaced review","Questions due in your review schedule are selected."],mistakes:["Mistake recovery","Unresolved questions return until the evidence improves."]};
const student=()=>access?.role==="student";
function updateModeCopy(){const[title,description]=modeCopy[UI.mode.value]||modeCopy.manual;document.getElementById("modeTitle").textContent=title;document.getElementById("modeDescription").textContent=description;UI.difficulty.disabled=UI.mode.value==="adaptive"}
function inferRowCourse(row){if(row.course_key)return ECHSPortalAccess.normaliseCourseKey(row.course_key);if(row.topic||row.unit||["CALCT3BC","ADAMS10","PEARSON_CH0"].includes(row.bank_code))return"ap-calculus";return""}
function rowsForGroup(groupKey){const rows=catalog.bundles[groupKey]||[];if(!student())return rows;return rows.filter(row=>access.courseKeys.includes(inferRowCourse(row)))}
function groupRows(){return ECHSBank.bundleGroups(catalog).filter(group=>!["blackboard_banks","source_chapters"].includes(group.key)||!student()).filter(group=>rowsForGroup(group.key).length)}
function displayBundleLabel(row){return ECHSBank.cleanStudentLabel(row?.label||"ECHS practice collection")}
function fillGroups(selected){const rows=groupRows(),chosen=rows.some(row=>row.key===selected)?selected:rows[0]?.key;UI.group.innerHTML=rows.map(group=>`<option value="${group.key}" ${group.key===chosen?"selected":""}>${ECHSBank.escape(group.label)}</option>`).join("");if(chosen)UI.group.value=chosen}
function unitGroupLabel(row){if(row.unit==null||row.unit==="")return null;const c=(row.course_key||"").toString();if(c.includes("calculus")&&!c.includes("pre"))return`AP Calculus · Unit ${row.unit}`;if(c.includes("precalculus"))return`AP Precalculus · Unit ${row.unit}`;if(c.includes("ib"))return`IB Math AI · Topic ${row.unit}`;return`Unit ${row.unit}`}
function bundleOption(row,chosen){return`<option value="${ECHSBank.escape(row.id)}" ${row.id===chosen?"selected":""}>${ECHSBank.escape(displayBundleLabel(row))} (${Number(row.count||0).toLocaleString()})</option>`}
function fillBundles(selectedId){const rows=rowsForGroup(UI.group.value),chosen=rows.some(row=>row.id===selectedId)?selectedId:rows[0]?.id;const grouped=rows.length&&rows.every(r=>r.unit!=null&&r.unit!=="");if(grouped){const buckets=new Map();rows.forEach(r=>{const g=unitGroupLabel(r)||"Other";if(!buckets.has(g))buckets.set(g,[]);buckets.get(g).push(r)});UI.bundle.innerHTML=[...buckets].map(([label,items])=>`<optgroup label="${ECHSBank.escape(label)}">${items.map(r=>bundleOption(r,chosen)).join("")}</optgroup>`).join("")}else{UI.bundle.innerHTML=rows.map(r=>bundleOption(r,chosen)).join("")}if(chosen)UI.bundle.value=chosen;updateBuilderContext()}
function updateBuilderContext(){const el=document.getElementById("builderCourseTag");if(!el)return;const course=currentCourse();const name=course?ECHSBank.cleanStudentLabel(ECHSBank.courseLabel(catalog,course)||course):"";const rows=rowsForGroup(UI.group.value);const total=rows.reduce((a,r)=>a+(Number(r.count)||0),0);el.textContent=name?`${name} · ${rows.length} option${rows.length===1?"":"s"} · ${total.toLocaleString()} questions`:""}
function currentBundle(){return rowsForGroup(UI.group.value).find(row=>row.id===UI.bundle.value)}
function currentCourse(){const row=currentBundle();return (row&&(row.course_key||inferRowCourse(row)))||params.get("course")||access?.courseKeys?.[0]||""}
function setBusy(value){loading=value;UI.start.disabled=value;UI.group.disabled=value;UI.bundle.disabled=value;UI.start.textContent=value?"Loading questions…":"Start focused practice"}
window.addEventListener("echs:bundle-progress",event=>{if(!loading)return;const{completed,total}=event.detail||{};if(total>1)UI.status.innerHTML=`<span class="pill">Loading practice ${completed} of ${total}…</span>`});
function unlockedTopics(course){const normal=ECHSPortalAccess.normaliseCourseKey(course);try{return new Set(JSON.parse(localStorage.getItem("echs_math_complete")||"[]").map(key=>String(key).split("::")).filter(parts=>parts.length>=4&&ECHSPortalAccess.normaliseCourseKey(parts[0])===normal).map(parts=>String(parts[2])))}catch{return new Set()}}
function lessonLocked(){return student()&&!assignmentId&&fromLesson&&!ECHSPortalAccess.lessonCompleted(fromLesson)}
function pathwayBanner(){const target=document.getElementById("pathwayBanner");if(lessonLocked()){target.innerHTML=`<div class="lockedPractice"><h2>Practice is still locked</h2><p>Complete the lesson before starting its focused practice.</p><a class="button wine" href="../index.html#courses">Return to learning path</a></div>`;UI.start.disabled=true;return}const course=params.get("course"),title=assignmentTitle||params.get("title");if(fromLesson||course)target.innerHTML=`<div class="pathwayBanner"><strong>${fromLesson?"Lesson practice unlocked":"Assigned-course practice"}</strong><br>${ECHSBank.escape(title||[course?ECHSBank.cleanStudentLabel(course):"",requestedTopic?`Skill ${requestedTopic}`:""].filter(Boolean).join(" · "))}</div>`;else target.innerHTML=""}
function assignmentBanner(){const target=document.getElementById("assignmentBanner");target.innerHTML=assignmentId?`<div class="notice"><strong>Teacher assignment:</strong> ${ECHSBank.escape(assignmentTitle||assignmentId)}. Results will synchronise with your learning record.</div>`:""}
function resumeBanner(){const target=document.getElementById("resumeBanner"),saved=ECHSLearning.getContinue();if(!saved||saved.type!=="practice"){target.innerHTML="";return}target.innerHTML=`<div class="resumeBanner"><div><strong>${ECHSBank.escape(saved.label||"Incomplete practice session")}</strong><p>Question ${(saved.index||0)+1} of ${saved.targetCount||saved.questionIds?.length||"—"} · updated ${new Date(saved.updatedAt).toLocaleString()}</p></div><div><a class="button wine" href="${ECHSBank.escape(saved.url||"practice.html?resume=1")}">Resume</a> <button class="button ghost" id="discardResume">Discard</button></div></div>`;document.getElementById("discardResume").onclick=()=>{ECHSLearning.clearContinue();target.innerHTML=""}}
async function loadCurrent(){const row=currentBundle();if(!row)return;setBusy(true);UI.status.innerHTML='<span class="pill">Loading ECHS practice…</span>';try{loaded=await ECHSBank.loadBundle(row);UI.heroLoaded.textContent=loaded.length.toLocaleString();const banks=[...new Set(loaded.map(question=>question.bank_code).filter(Boolean))].sort();UI.heroBanks.textContent=banks.length.toLocaleString();const wanted=params.get("bank")||UI.bank.value;UI.bank.innerHTML='<option value="all">All ECHS banks</option>'+banks.map(code=>`<option value="${ECHSBank.escape(code)}" ${code===wanted?"selected":""}>${ECHSBank.escape(ECHSBank.bankLabel(code,currentCourse()))}</option>`).join("");const sections=new Map();loaded.forEach(question=>{const value=String(question.source?.section||"unmapped"),title=question.source?.section_title||question.source?.skill_title||"";sections.set(value,value==="unmapped"?"General practice":`${value}${title?` · ${title}`:""}`)});UI.section.innerHTML='<option value="all">All practice sections</option>'+[...sections].sort((a,b)=>a[0].localeCompare(b[0],undefined,{numeric:true})).map(([value,label])=>`<option value="${ECHSBank.escape(value)}">${ECHSBank.escape(ECHSBank.cleanStudentLabel(label))}</option>`).join("");UI.status.innerHTML=`<span class="pill teal">${loaded.length.toLocaleString()} questions available</span><span class="pill wine">${ECHSBank.escape(displayBundleLabel(row))}</span>`}catch(error){UI.status.innerHTML='<span class="pill red">Practice unavailable</span>';UI.shell.innerHTML=`<div class="notice"><strong>Could not load this practice set.</strong><br>${ECHSBank.escape(error.message)}</div>`;throw error}finally{setBusy(false);if(lessonLocked())UI.start.disabled=true}}
function filters(){return{bank:UI.bank.value,type:UI.type.value,difficulty:UI.mode.value==="adaptive"?"all":UI.difficulty.value,section:UI.section.value,topic:requestedTopic||null}}
function hydrateAssets(root){if(window.ECHSBlackboardAssets)ECHSBlackboardAssets.hydrate(root).catch(error=>console.warn(error))}
function scrollQuestionIntoView(){UI.shell.scrollIntoView({behavior:matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth",block:"start"})}
function sessionURL(){const query=new URLSearchParams({resume:"1",bundle:currentBundle()?.id||"",mode:UI.mode.value});["course","unit","topic","from","title"].forEach(key=>{if(params.get(key))query.set(key,params.get(key))});if(assignmentId)query.set("assignment",assignmentId);return new URL(`practice.html?${query}`,location.href).href}
function persistContinue(){if(!session)return;ECHSLearning.patchSession(session.id,{questionIds:set.map(q=>q.id),answered:state.graded,correct:state.correct,index:state.index});ECHSLearning.setContinue({type:"practice",label:`${modeCopy[UI.mode.value]?.[0]||"Practice"} · ${displayBundleLabel(currentBundle())}`,url:sessionURL(),bundleId:currentBundle()?.id,group:UI.group.value,mode:UI.mode.value,questionIds:set.map(q=>q.id),index:state.index,targetCount,correct:state.correct,graded:state.graded,answeredIds:[...state.answered],sessionId:session.id,assignmentId})}
function render(){const question=set[state.index];if(!question){UI.shell.innerHTML='<div class="empty"><div class="emptyState"><div class="emptyStateIcon">!</div><h2>No unlocked questions yet</h2><p>Complete the related lesson or ask your teacher to assign a focused activity.</p><a class="button wine" href="../index.html#courses">Return to learning path</a></div></div>';return}state.response=null;state.checked=false;questionStartedAt=Date.now();const choices=ECHSBank.choiceOrder(question),classification=question.classification||{},source=question.source||{},auto=ECHSBank.isAutoGradable(question),responseHTML=["mcq","true_false"].includes(question.type)?`<div class="choices">${choices.map((choice,index)=>`<button class="choice" data-id="${ECHSBank.escape(choice.id)}"><span class="choiceLabel">${String.fromCharCode(65+index)}</span><span>${choice.html}</span></button>`).join("")}</div>`:question.type==="fill_blank"?'<div class="control" style="margin-top:1rem"><label for="answerInput">Your answer</label><input id="answerInput" autocomplete="off"></div>':'<div class="control" style="margin-top:1rem"><label for="answerInput">Your response</label><textarea id="answerInput" rows="7"></textarea></div>';UI.shell.innerHTML=`<article class="questionCard"><div class="pillRow"><span class="pill wine">Question ${state.index+1} of ${targetCount}</span><span class="pill teal">${ECHSBank.escape(ECHSBank.bankLabel(question.bank_code,currentCourse()))}</span><span class="pill gold">${ECHSBank.escape(modeCopy[UI.mode.value]?.[0]||"Practice")}</span>${classification.ap_topic?`<span class="pill">Skill ${ECHSBank.escape(classification.ap_topic)}</span>`:""}<span class="pill">${ECHSBank.escape(ECHSBank.labelType(question.type))}</span></div><div class="progressTrack"><i style="width:${((state.index+1)/targetCount)*100}%"></i></div><h2>${ECHSBank.escape(ECHSBank.cleanStudentLabel(classification.ap_topic_title||source.skill_title||source.section_title||question.pool_title||"Practice question"))}</h2><div class="prompt">${question.prompt_html}</div>${responseHTML}<div id="feedback" class="feedback" aria-live="polite"></div><div class="questionFooter"><button class="button primary" id="check">${auto?"Check answer":"Reveal feedback"}</button><div><button class="button ghost" id="prev" ${state.index===0?"disabled":""}>Back</button> <button class="button wine" id="next">${state.index===targetCount-1?"Finish":"Next"}</button></div></div></article>`;hydrateAssets(UI.shell);if(["mcq","true_false"].includes(question.type))document.querySelectorAll(".choice").forEach(button=>button.onclick=()=>{if(state.checked)return;document.querySelectorAll(".choice").forEach(item=>item.classList.remove("selected"));button.classList.add("selected");state.response=button.dataset.id});document.getElementById("check").onclick=()=>check(question);document.getElementById("prev").onclick=()=>{if(state.index>0){state.index--;render();scrollQuestionIntoView();persistContinue()}};document.getElementById("next").onclick=nextQuestion;persistContinue()}
function check(question){if(state.checked)return;if(!["mcq","true_false"].includes(question.type))state.response=document.getElementById("answerInput")?.value||"";const auto=ECHSBank.isAutoGradable(question),correct=auto?ECHSBank.answerIsCorrect(question,state.response):null;state.checked=true;lastResult=correct;if(auto&&!state.answered.has(question.id)){state.graded++;if(correct)state.correct++;state.answered.add(question.id);ECHSBank.saveAttempt(question,correct,state.response,{mode:UI.mode.value,sessionId:session?.id,assignmentId,durationMs:Date.now()-questionStartedAt})}if(["mcq","true_false"].includes(question.type))document.querySelectorAll(".choice").forEach(button=>{if((question.correct_choice_ids||[]).includes(button.dataset.id))button.classList.add("correct");else if(button.dataset.id===state.response)button.classList.add("incorrect");button.disabled=true});const feedback=document.getElementById("feedback");feedback.className=`feedback show ${correct===false?"incorrect":"correct"}`;const accepted=(question.accepted_answers||[]).join(" / "),adaptiveNote=UI.mode.value==="adaptive"&&auto?`<p><b>Next step:</b> ${correct?"difficulty may increase or the skill may broaden":"the next question will reinforce the prerequisite level"}.</p>`:"";feedback.innerHTML=`<strong>${auto?(correct?"Correct.":"Not correct. This question was added to your review queue."):"Compare your response with the available feedback."}</strong>${adaptiveNote}${accepted?`<p><b>Accepted answer:</b> ${ECHSBank.escape(accepted)}</p>`:""}${question.solution_html?`<div class="solution">${question.solution_html}</div>`:'<div class="solution">A detailed worked solution is not yet available for this item.</div>'}`;hydrateAssets(feedback);persistContinue()}
function nextQuestion(){if(state.index<set.length-1){state.index++;render();scrollQuestionIntoView();return}if(UI.mode.value==="adaptive"&&set.length<targetCount){const candidates=ECHSLearning.selectAdaptive(ECHSBank.filterQuestions(loaded,filters()),1,{excludedIds:set.map(q=>q.id),lastCorrect:lastResult});if(candidates.length){set.push(candidates[0]);state.index++;render();scrollQuestionIntoView();return}}finish()}
function finish(){const percentage=state.graded?Math.round(state.correct/state.graded*100):null;if(session)ECHSLearning.endSession(session.id,{questionIds:set.map(q=>q.id),answered:state.graded,correct:state.correct,score:percentage,assignmentId});ECHSLearning.clearContinue();UI.shell.innerHTML=`<div class="result"><div class="resultScore">${percentage==null?"Practice complete":`${state.correct} / ${state.graded} (${percentage}%)`}</div><p>Mastery evidence, review timing and recovery priorities were updated automatically.</p><div class="heroActions"><a class="button wine" href="../index.html#courses">Return to learning path</a><a class="button ghost" data-role-home href="student.html">Open dashboard</a><a class="button ghost" href="mistakes.html">Review mistakes</a></div></div>`;resumeBanner()}
function eligibleRows(){let rows=ECHSBank.filterQuestions(loaded,filters()),mode=UI.mode.value;if(student()&&!assignmentId){const hasApTopics=loaded.some(q=>q.classification?.ap_topic);if(hasApTopics){const course=params.get("course")||access.courseKeys[0],topics=unlockedTopics(course);rows=rows.filter(question=>topics.has(String(question.classification?.ap_topic||"")))}}if(mode==="review"){const ids=new Set(ECHSLearning.dueReviews().map(row=>row.questionId));rows=rows.filter(q=>ids.has(String(q.id)))}if(mode==="mistakes"){const ids=new Set(ECHSLearning.mistakes().map(row=>row.questionId));rows=rows.filter(q=>ids.has(String(q.id)))}return rows}
async function start(){if(lessonLocked())return;const rows=eligibleRows();targetCount=Math.min(rows.length,Number(UI.count.value)||10);lastResult=null;set=UI.mode.value==="adaptive"?ECHSLearning.selectAdaptive(rows,Math.min(1,targetCount)):ECHSBank.shuffle(rows).slice(0,targetCount);state={index:0,response:null,checked:false,correct:0,graded:0,answered:new Set()};session=ECHSLearning.startSession({type:"practice",mode:UI.mode.value,bundleId:currentBundle()?.id,bundleLabel:displayBundleLabel(currentBundle()),questionIds:set.map(q=>q.id),targetCount,assignmentId,title:assignmentTitle});UI.status.innerHTML=`<span class="pill teal">${rows.length.toLocaleString()} unlocked questions</span><span class="pill wine">${targetCount.toLocaleString()} planned</span><span class="pill gold">${ECHSBank.escape(modeCopy[UI.mode.value]?.[0])}</span>`;render();scrollQuestionIntoView();resumeBanner()}
function restore(){const saved=ECHSLearning.getContinue();if(!saved||saved.type!=="practice"||saved.bundleId!==currentBundle()?.id)return false;UI.mode.value=saved.mode||"manual";updateModeCopy();targetCount=Number(saved.targetCount)||saved.questionIds?.length||10;const map=new Map(loaded.map(q=>[String(q.id),q]));set=(saved.questionIds||[]).map(id=>map.get(String(id))).filter(Boolean);if(!set.length)return false;state={index:Math.min(Number(saved.index)||0,set.length-1),response:null,checked:false,correct:Number(saved.correct)||0,graded:Number(saved.graded)||0,answered:new Set(saved.answeredIds||[])};session=ECHSLearning.activeSession(saved.sessionId)||ECHSLearning.startSession({type:"practice",mode:UI.mode.value,bundleId:saved.bundleId,questionIds:set.map(q=>q.id),targetCount,assignmentId:saved.assignmentId});render();scrollQuestionIntoView();return true}
(async()=>{try{access=await ECHSPortalAccess.ready;if(!access?.authenticated)return;if(student()){document.body.classList.add("studentFocused");if(!params.get("course")&&access.courseKeys[0]){params.set("course",access.courseKeys[0]);const next=new URL(location.href);next.searchParams.set("course",access.courseKeys[0]);history.replaceState(null,"",next);}const requested=params.get("course");if(requested&&!ECHSPortalAccess.courseAllowed(requested,access)){UI.shell.innerHTML='<div class="lockedPractice"><h2>This course is not assigned to your account</h2><p>Return to your dashboard or ask your teacher to update the class roster.</p></div>';return}UI.mode.value=params.get("mode")||"adaptive"}catalog=await ECHSBank.loadCatalog();const selected=ECHSBank.selectedBundleFromParams(catalog);fillGroups(selected.group);fillBundles(selected.row?.id);UI.mode.value=params.get("mode")||UI.mode.value||"manual";if(params.get("count"))UI.count.value=params.get("count");updateModeCopy();await loadCurrent();pathwayBanner();assignmentBanner();resumeBanner();const learning=ECHSLearning.summary();UI.heroDue.textContent=learning.due.toLocaleString();UI.heroMastery.textContent=learning.mastered.toLocaleString();UI.group.onchange=async()=>{fillBundles();await loadCurrent()};UI.bundle.onchange=loadCurrent;UI.mode.onchange=updateModeCopy;UI.start.onclick=()=>start().catch(error=>UI.shell.innerHTML=`<div class="notice">${ECHSBank.escape(error.message)}</div>`);if(params.get("resume")==="1"){if(!restore())resumeBanner()}else if(params.get("autostart")==="1"&&!lessonLocked())start()}catch(error){UI.shell.innerHTML=`<div class="notice">${ECHSBank.escape(error.message)}</div>`}})();
